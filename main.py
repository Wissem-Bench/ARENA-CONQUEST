from game import Game

def main():
    game = Game()
    game.run()

if __name__ == "__main__":
    main()

    # todo : sprint [ok] , heroknight animmation [ok] , start/finish-point animation [ok]
    #        health-bar offset [ok], health error [ok], animations offset , heroes scaling
    #        xy correction[ok] , change menu bg [ok], other enemies
    #        bg collision , steps and transparency [ok]
    #        score/Xp , camera [ok]

    # todo 20/05 : animations aseprite(offset&scaling), heroes.selfparams bg_movingzone
    #              minotor(/other one), game_over &score/xp &hurt_obj , VSPlayer(akrem)